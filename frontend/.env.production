# SmartTravel AI — Frontend Production Environment
# Frontend and backend are served from the same Render domain.
# Using relative /api path — no VITE_API_BASE_URL needed.
# VITE_API_BASE_URL intentionally left unset so api.js falls back to "/api"
